---
status: '{proposed | accepted | rejected | deprecated | superseded by [題](YYYY-MM-DD-slug.md)}'
date: {YYYY-MM-DD}
decision-makers: '{判断を持つ人}'
---

# {判断を表す短い題。問題ではなく、決めたことを書く}

## 文脈と課題

{なぜ今この判断が要るのか。何が壊れているか、何が変わるか、放置すると何が壊れるか。
どんな制約があるか。初めて読む人（やエージェント）が、追加で誰にも訊かずに
理解できるだけの背景を書く。既存の方向性メモや AGENTS.md の該当節にリンクする。}

## 判断

{何をすることにしたか。範囲と非目標を含めて具体的に書く。
「X を使う」ではなく「Y のために X を使い、Z はしない」まで書く。}

## 帰結

- 良い点: {具体的に}
- 悪い点: {具体的に。すべて良い点なら、それは書けていない}
- どちらでもない点: {代償として受け入れたこと}

## 実装計画

- **影響範囲**: {ファイル・ディレクトリ。「データ層」ではなく `server/internal/domain/auth/`}
- **依存**: {足す・消す・上げるパッケージとバージョン}
- **倣う型**: {既存コードのどの型に合わせるか。抽象的な説明ではなく実在の場所を指す}
- **避けること**: {やってはいけないこと。なぜ駄目かも一行で}
- **移行**: {既存データ・既存呼び出し元をどうするか。無ければ「不要」と書く}

### 検証

{実際に走るコマンドで書く。「動くこと」は検証条件ではない。}

- [ ] {例: `make test` が通る}
- [ ] {例: `golangci-lint run ./...` が 0 issues}
- [ ] {例: 使い捨て DB に `migrate up` → `down -all` → `up` が通る}
- [ ] {構造の検証も入れる。例: 規則が 1 か所にしか書かれていない}

<!-- 任意 — 使わないなら消す -->

## 却下した案

- {案 1}: {なぜ落としたか。1〜2 文}
- {案 2}: {なぜ落としたか}

<!-- 任意 — 使わないなら消す -->

## 補足

{関連 ADR / 方向性メモ / PR / Issue。再考の引き金になる条件。
受理後の学びは日付付きでここに追記する（過去は書き換えない）。}
