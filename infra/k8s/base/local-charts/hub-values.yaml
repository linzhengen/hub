replicaCount: 2

initContainers:
  - name: wait-for-db
    image: busybox:1.36
    command:
      - sh
      - -c
      - |
        until nc -z postgresql.postgresql.svc.cluster.local 5432; do
          echo "waiting for database..."
          sleep 2
        done
