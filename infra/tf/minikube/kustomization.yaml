apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: terraform-config-minikube
    namespace: keycloak
    options:
      disableNameSuffixHash: true
    files:
      - main.tf
      - variables.tf
      - provider.tf
      - backend.tf
      - versions.tf
